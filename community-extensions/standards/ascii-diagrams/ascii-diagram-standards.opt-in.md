# ASCII Diagram Standards — Opt-In

**Extension**: ASCII Diagram Standards

## Opt-In Prompt

The following question is automatically included during Requirements Analysis:

```markdown
## Question: ASCII Diagram Standards
Should the ASCII diagram pattern library and strict validation be enforced for this project?

A) Yes — enforce full ASCII diagram standards (recommended for projects that include architecture or flow diagrams in documentation)
B) No — use basic ASCII validation rules only (character set and width rules remain enforced by default)
X) Other (please describe after [Answer]: tag below)

[Answer]:
```
